//Интерфейс с одним методом execute(), который будет выполнять и который называется в
// терминах паттерна "команда (command)"

package ArchitectureSWHW2.Command;

public interface Command {
    void execute();
}
